apiVersion: v1
kind: ReplicationController 
metadata:
  name: durgaarc
spec:
  replicas: 3
  selector:
    app: durgaapp
  template:
    metadata:
      labels:
        app: durgaapp
    spec:
      containers: 
      - name: myc11
        image: nginx
        ports:
        - containerPort: 8080

   